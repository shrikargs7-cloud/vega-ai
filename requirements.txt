# No external dependencies required for basic functionality
# Your server.py uses only Python standard library

# Optional: If you want to enable Gemini AI Vision features in the future, uncomment:
# google-generativeai>=0.3.0

# Note: Your current server.py works without any packages
# The frontend loads Firebase, Chart.js, html2pdf, and Font Awesome via CDN

google-generativeai>=0.3.0
